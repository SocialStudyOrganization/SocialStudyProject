<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ndlp.socialstudy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.ndlp.socialstudy.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".activity.AppLogoFirstPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginSystem.LoginActivity" />
        <activity android:name=".LoginSystem.RegisterActivity" />
        <activity android:name=".NavigationDrawer_BottomNavigation.AboutUsActivity" />
        <activity android:name=".NavigationDrawer_BottomNavigation.FeedbackActivity" />
        <activity android:name=".NavigationDrawer_BottomNavigation.FehlerMeldenAcivity" />
        <activity android:name=".tutorialsheets.TutorialsheetsSlider" />
        <activity android:name=".NavigationDrawer_BottomNavigation.MainActivity" />
        <activity
            android:name=".Umfragen.NewUmfrageActivity"
            android:theme="@style/Theme.AppCompat.Transparent" />
        <activity
            android:name=".Umfragen.OptionPollActivity"
            android:theme="@style/Theme.AppCompat.Transparent" />
        <activity android:name=".activity.PDFViewerActivity"></activity>
    </application>

</manifest>